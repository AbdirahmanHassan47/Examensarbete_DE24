version: 2

sources:
  - name: sl
    database: TRAFIK_DATA
    schema: STAGING
    tables:
      - name: sl_departures_raw
        description: "Raw SL departures loaded by dlt"

models:
  - name: stg_sl_departures
    description: "Staging view over raw SL departures"
    columns:
      - name: _site_id
        tests:
          - not_null
      - name: _ingested_at
        tests:
          - not_null
